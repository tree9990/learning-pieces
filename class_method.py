class Luxry_Watch():    watches_created = 0    def __init__(self):        Luxry_Watch.watches_created += 1    @classmethod     def get_number_of_watches_created(cls):        return '# of watches created: {}'.format(cls.watches_created)        @classmethod    def watches_engrave(cls, engrave):        _watch = cls ()        _watch.engrave = engrave        return _watch    @staticmethod     def validate(engrave):        if len(engrave) <= 40:            return True        else:            return False            w1 = Luxry_Watch()eng_list = ['This is for a test', 'foo@baz.com', 'hello'*41]for element in eng_list:    if Luxry_Watch.validate(element):        wat = Luxry_Watch.watches_engrave(element)        print ('a watch with engrave created')    else:        print ('The engraved words', element, 'is too long')        print ('\n')print (Luxry_Watch.get_number_of_watches_created())